{
    "name": "creddi",
    "version": "1.0",
    "scripts": {
        "postinstall": "cd api && npm i && npm install -g ts-node && npm install @types/node --save-dev",
        "build": "cd api && npm start"
    }
}